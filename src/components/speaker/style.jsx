import styled from '@emotion/styled';
import theme from '../../theme-config';

const SpeakerWrapper = styled('div')`
  label: speaker-wrapper;
  color: ${theme.gray};
  text-align: center;
`;

export { SpeakerWrapper };
